{
  "manifest_version": 3,
  "name": "Append URL to Title",
  "version": "1.2",
  "description": "Automatically appends the current page's URL to the title.",
  "permissions": ["scripting", "activeTab", "webNavigation"],
  "background": {
    "service_worker": "background.js"
  },
  "host_permissions": ["<all_urls>"]
}
